pub trait Resource {
    // TODO
}

pub struct BaseResource {
    id: u32,
    name: String,
}

pub struct Labor {
    id: u32,
    name: String,
}

pub struct Material {
    id: u32,
    name: String,
}

pub struct CapitalGood {
    id: u32,
    name: String,
}

pub struct Crop {
    id: u32,
    name: String,
}

pub struct Livestock {
    id: u32,
    name: String,
}

