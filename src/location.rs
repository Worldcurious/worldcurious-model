pub trait Location {
    // TODO
}

pub struct Faction {
    id: u32,
    name: String,
    population: u64,
}

pub struct Region {
    id: u32,
    name: String,
    population: u64,
}

pub struct City {
    id: u32,
    name: String,
    population: u64,
}